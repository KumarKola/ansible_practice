---
- hosts: all
  become: true
  tasks:

    - name: update local index
      apt:
        update_cache: yes


    - name: install apache server on all nodes
      apt:
        name:
          - apache2
          - libapache2-mod-php


